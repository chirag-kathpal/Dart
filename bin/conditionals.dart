void main() {
  // #Conditional Expression
  var is_login = true;

  // ## condition ? exprsn1 : eprsn2 ;
  var user = is_login ? 'Chirag' : 'Guest';
  print(user);

  // var name = 'Chirag';
  var name = null;
  var ans = name ?? 'Guest';
  // ## if not null print variable ,otherwise print the expression

  // # Control Flow Statements
  // ## if statement
  // ##if else
  // ## if else if

}
